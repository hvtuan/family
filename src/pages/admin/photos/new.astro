---
// Legacy /admin/photos/new → /admin/media/new (M2 of media-v2 hub).
// The new path uses uploadPhotoMedia (3 variants + EXIF strip) instead
// of the single-blob legacy uploadPhotoFile.
export const prerender = false;
return Astro.redirect("/admin/media/new", 301);
---
