---
// Legacy /admin/photos/[id] → /admin/media/[id]. The new hub owns the
// edit experience; this stays as a permanent redirect for old links.
export const prerender = false;
const id = Astro.params.id!;
return Astro.redirect(`/admin/media/${id}`, 301);
---
